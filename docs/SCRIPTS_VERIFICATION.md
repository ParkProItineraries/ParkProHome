# ParkProHome - Scripts Usage Verification

**Generated**: 2025-01-27  
**Purpose**: Verify usage of scripts in `/scripts/` folder

## Verification Methodology

For each script, checked:
1. `package.json` scripts section
2. `.github/workflows/` CI/CD files
3. `deploy.sh` and `deploy-to-existing-s3.sh` deployment scripts
4. General repository references (imports/requires/calls)

## Scripts Analysis

### 1. `scripts/accessibility-check.js`

**Purpose**: Scans pages for common accessibility issues (missing alt text, H1 tags, aria-labels, focus states)

**References Found**:
- ❌ Not in `package.json` scripts
- ❌ Not in `.github/workflows/deploy.yml`
- ❌ Not in `deploy.sh`
- ❌ Not in `deploy-to-existing-s3.sh`
- ✅ Only mentioned in archived docs (`REFACTORING_SUMMARY.md`, `REFACTORING_CHANGELOG.md`)

**Classification**: **QUARANTINE** - Utility script, not actively used

**Action**: Move to `__graveyard__/scripts-unused/`

---

### 2. `scripts/audit-routes.js`

**Purpose**: Analyzes all routes, pages, links, and dependencies to identify active routes, unused pages, etc.

**References Found**:
- ❌ Not in `package.json` scripts
- ❌ Not in `.github/workflows/deploy.yml`
- ❌ Not in `deploy.sh`
- ❌ Not in `deploy-to-existing-s3.sh`
- ✅ Only mentioned in archived docs (`REFACTORING_SUMMARY.md`, `REFACTORING_CHANGELOG.md`)

**Classification**: **QUARANTINE** - Utility script, not actively used

**Action**: Move to `__graveyard__/scripts-unused/`

---

### 3. `scripts/verify-centralized-copy.js`

**Purpose**: Checks which pages are using centralized copy from strings.js vs hardcoded marketing text

**References Found**:
- ❌ Not in `package.json` scripts
- ❌ Not in `.github/workflows/deploy.yml`
- ❌ Not in `deploy.sh`
- ❌ Not in `deploy-to-existing-s3.sh`
- ✅ Only mentioned in archived docs (`REFACTORING_SUMMARY.md`, `REFACTORING_CHANGELOG.md`)

**Classification**: **QUARANTINE** - Utility script, not actively used

**Action**: Move to `__graveyard__/scripts-unused/`

---

### 4. `scripts/audit-output/` Folder

**Contents**:
- `accessibility.json` - Generated by accessibility-check.js
- `copy-compliance.json` - Generated by verify-centralized-copy.js
- `routes.json` - Generated by audit-routes.js

**Classification**: **GENERATED OUTPUT**

**Action**: Add to `.gitignore` (these are generated files, not source files)

---

## Deployment Scripts

### `deploy.sh`

**Purpose**: Full deployment script with CloudFront invalidation

**References Found**:
- ✅ Used in CI/CD (`.github/workflows/deploy.yml` uses similar logic)
- ✅ Comprehensive deployment script

**Classification**: **KEEP** - Active deployment script

---

### `deploy-to-existing-s3.sh`

**Purpose**: Simple S3 deployment script for existing buckets

**References Found**:
- ❌ Not in CI/CD
- ❌ Not referenced elsewhere
- ✅ Simpler alternative to `deploy.sh`

**Classification**: **KEEP** - Alternative deployment script (may be used manually)

**Note**: Both deployment scripts serve different purposes:
- `deploy.sh` - Full deployment with CloudFront
- `deploy-to-existing-s3.sh` - Simple S3-only deployment

---

## Summary

| Script | Status | Action |
|--------|--------|--------|
| `accessibility-check.js` | QUARANTINE | Move to `__graveyard__/scripts-unused/` |
| `audit-routes.js` | QUARANTINE | Move to `__graveyard__/scripts-unused/` |
| `verify-centralized-copy.js` | QUARANTINE | Move to `__graveyard__/scripts-unused/` |
| `audit-output/` | GENERATED | Add to `.gitignore` |
| `deploy.sh` | KEEP | No action |
| `deploy-to-existing-s3.sh` | KEEP | No action |
