# ParkPro Home - Environment Variables

# ============================================================================
# NO ENVIRONMENT VARIABLES REQUIRED! 🎉
# ============================================================================
#
# This application fetches ALL configuration from the backend API,
# which loads values from AWS Systems Manager (SSM) Parameter Store.
#
# Configuration is fetched from: GET /api/config
# 
# The backend (ParkProBackend) loads from SSM:
#   - API_URL (from /parkpro/API_URL)
#   - STRIPE_PUBLISHABLE_KEY (from /parkpro/STRIPE_PUBLISHABLE_KEY)
#
# ============================================================================
# LOCAL DEVELOPMENT
# ============================================================================
#
# The app automatically detects localhost and uses:
#   - API URL: http://localhost:3001
#   - Stripe Key: Fetched from backend
#
# Just run: npm run dev
#
# ============================================================================
# PRODUCTION DEPLOYMENT
# ============================================================================
#
# NO environment variables needed in your hosting platform!
#
# The app automatically uses:
#   - API URL: https://api.parkproit.com
#   - Stripe Key: Fetched from backend
#
# Just run: npm run build && deploy dist/
#
# ============================================================================
# OPTIONAL: Analytics (silences warnings)
# ============================================================================

# Google Analytics 4 Measurement ID (optional)
# VITE_GA4_MEASUREMENT_ID=G-XXXXXXXXXX

# ============================================================================
# That's it! All other config comes from AWS SSM via the backend API.
# ============================================================================
